import { createSlice } from "@reduxjs/toolkit";

const CampanySlice = createSlice({
  name: "CampanySlice",
  initialState: {
    name: "",
    email: "",
  },
  reducers: {},
});

export default CampanySlice;
